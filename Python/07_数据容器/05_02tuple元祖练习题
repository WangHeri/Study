"""练习题"""
"""
定义一个元组，内容是：（'周杰轮', 11, ['football', 'music']），记录的是一个学生的信息（姓名、年龄、爱好）
请通过元组的功能（方法），对其进行
	1. 查询其年龄所在的下标位置
	2. 查询学生的姓名
	3. 删除学生爱好中的football
	4. 增加爱好:coding到爱好list内
"""

a = ('周杰轮', 11, ['football', 'music'])

print(a.index(11)) #查询其年龄所在的下标位置
print(a[0]) #查询学生的姓名
del a[2][0] #删除元组里的列表的内容的两种方式之一
a[2].pop(0) #删除元组里的列表的内容的两种方式之一
a[2].append("coding") #增加爱好:coding到爱好list内

print(a)
